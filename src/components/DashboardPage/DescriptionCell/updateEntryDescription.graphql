mutation UpdateEntryDescription($id: ID!, $description: String!) {
  updateEntry(id: $id, description: $description) {
    entry {
      id
      description
      amountCents
      date
      isNew
    }
  }
}
