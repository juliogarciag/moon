mutation UpdateEntryAmountCents($id: ID!, $amountCents: Int!) {
  updateEntry(id: $id, amountCents: $amountCents) {
    entry {
      id
      description
      amountCents
      date
      isNew
    }
  }
}
